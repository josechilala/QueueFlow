# Public HTTPS API origin, included in the static output at build time.
# Configure the marketing site's origin in the API's Cors:Origins as well.
# For next dev only, http://localhost:5260 is also accepted.
NEXT_PUBLIC_QUEUEFLOW_API_URL=
