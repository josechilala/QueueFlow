import { getPlatformOrganizations } from '../../../../lib/server-platform';
export default async function OrganizationsPage() { const result = await getPlatformOrganizations(); return <><div className="page-heading"><h1>Empresas</h1></div><section className="table-card"><table><thead><tr><th>Empresa</th><th>Owner</th><th>Plano</th><th>Unidades</th><th>Usuários</th></tr></thead><tbody>{result.data?.map(item => <tr key={item.id}><td><strong>{item.name}</strong><br /><small>{item.slug}</small></td><td>{item.ownerEmail ?? '—'}</td><td>{item.plan ?? '—'}<br /><small>{item.subscriptionStatus ?? '—'}</small></td><td>{item.branchCount}</td><td>{item.userCount}</td></tr>)}</tbody></table>{!result.data?.length && <p className="muted">Nenhuma organização encontrada.</p>}</section></>; }
