'use client';
import { useState } from 'react'; import { useRouter } from 'next/navigation'; import type { AppointmentStatus } from '../../../lib/server-appointments';
export function AppointmentActions({ id, status }: { id: string; status: AppointmentStatus }) { const router = useRouter(); const [error, setError] = useState(''); const [pending, setPending] = useState(false); async function act(action: string) { const reason = ['cancel', 'no-show'].includes(action) ? window.prompt('Motivo (opcional):') : null; setPending(true); const response = await fetch(`/api/appointments/${id}/${action}`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ reason }) }); if (!response.ok) { const body = await response.json().catch(() => null); setError(body?.detail ?? 'Não foi possível concluir a ação.'); setPending(false); return; } router.refresh(); setPending(false); } return <div className="form-actions">{status === 'Scheduled' && <button disabled={pending} onClick={() => act('confirm')}>Confirmar</button>}{['Scheduled', 'Confirmed'].includes(status) && <button className="danger-link" disabled={pending} onClick={() => act('cancel')}>Cancelar</button>}{status === 'Confirmed' && <button className="secondary" disabled={pending} onClick={() => act('no-show')}>Marcar no-show</button>}{error && <p className="form-error">{error}</p>}</div>; }
