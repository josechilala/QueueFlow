import Link from 'next/link'; import { notFound, redirect } from 'next/navigation'; import { AdminShell } from '../../../components/admin-shell'; import { authFailure } from '../../../lib/auth'; import { getAppointment } from '../../../lib/server-appointments'; import { getServerSession } from '../../../lib/server-session'; import { AppointmentActions } from './appointment-actions';
export default async function AppointmentPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const session = await getServerSession(); if (authFailure(session.status) === 'unauthorized') redirect(`/api/auth/refresh?returnTo=/appointments/${id}`); if (!session.user) notFound(); const result = await getAppointment(id); if (result.status === 404) notFound(); if (!result.data) throw new Error('Não foi possível carregar o agendamento.'); const item = result.data; return <AdminShell user={session.user}><div className="section-heading"><div><Link href="/appointments">← Agendamentos</Link><h3>{item.customerName}</h3><p className="muted">{item.serviceName} · {item.branchName}</p></div></div><section className="notice appointment-details"><dl><div><dt>Data</dt><dd>{new Date(item.scheduledStart).toLocaleString('pt-BR')}</dd></div><div><dt>Status</dt><dd>{item.status}</dd></div><div><dt>Telefone</dt><dd>{item.customerPhone ?? '—'}</dd></div><div><dt>E-mail</dt><dd>{item.customerEmail ?? '—'}</dd></div><div><dt>Código</dt><dd>{item.confirmationCode}</dd></div></dl><AppointmentActions id={item.id} status={item.status} /></section><div className="table-wrap"><table><thead><tr><th>Quando</th><th>Anterior</th><th>Novo</th><th>Motivo</th></tr></thead><tbody>{item.history.map((history, index) => <tr key={`${history.createdAt}-${index}`}><td>{new Date(history.createdAt).toLocaleString('pt-BR')}</td><td>{history.previousStatus}</td><td>{history.newStatus}</td><td>{history.reason ?? '—'}</td></tr>)}</tbody></table></div></AdminShell>; }
